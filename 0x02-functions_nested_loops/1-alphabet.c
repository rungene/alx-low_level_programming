#include "main.h"
/**
 * print_alphabet -  prints the lowercase alphabet
 *
 * Return: void
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; j <= 'z'; j++)
		_putchar(i);
	_putchar('\n');
	return (0);
}
